# Example environment variables for local development and for the deploy provider.
# Copy this file to `.env` locally and fill in real values. Do NOT commit real secrets.

# This project does not use any third-party API keys by default.
# If you later decide to enable external services, add the required keys here.
# Example environment variables
# Copy to `.env.local` or `.env` and fill in secrets as needed. Do NOT commit real secrets.

# NODE_ENV is set automatically but can be used in local env if desired
NODE_ENV=development

# Example: API key for optional backends (leave empty if unused)
# EXAMPLE_API_KEY=
# Example environment variables for local development
# Copy this file to `.env.local` or set env vars in your environment. Do NOT commit real secrets.

# No third-party API keys are configured by default.
# VITE_OPENAI_API_KEY=

# Public base path (if you deploy to GitHub Pages or a subpath)
VITE_PUBLIC_BASE_PATH=/

# Optional: local dev server port
PORT=5173
